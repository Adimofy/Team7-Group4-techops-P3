pipeline {
    agent any
    stages {
        stage('jonas') {
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl'
            }
        }
        stage('prosper') {
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl'
            }
        }
        stage('erica') {
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl'
            }
        }
        stage('bill') {
            steps {
                sh 'ps -ef'
                sh 'sudo systemctl'
            }
        }
    }
}